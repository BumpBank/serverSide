var test = require('tape')

test('testing bumpbank', function (t) {
  t.ok(true)
  t.end()
})

test('GOOD_LOGIN loads login page', (t) => {
  t.ok()
  t.end()
})

test('LOAD_BAL loads balance view', (t) => {
  t.ok()
  t.end()
})
